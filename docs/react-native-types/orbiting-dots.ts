import { ViewStyle } from "react-native";

export interface OrbitDotLoaderProps {
  dotColor?: string;
  dotRadius?: number;
  centerRadius?: number;
  size?: number;
  duration?: number;
  numDots?: number;
  style?: ViewStyle;
}
